USE COMPANYDB;

SELECT COUNT( * ) AS NUM_ROWS FROM EMPLOYEE;

SELECT * FROM EMPLOYEE;

SELECT SUM(SALARY) AS EXPENDITURE, SEX 
FROM EMPLOYEE 
GROUP BY SEX;

SELECT MAX(SALARY) AS DIVERSITY, SEX 
FROM EMPLOYEE 
GROUP BY SEX;

SELECT AVG(SALARY) AS AVERAGE, SEX 
FROM EMPLOYEE 
GROUP BY SEX;

SELECT * FROM EMPLOYEE;

-- LOLOL
SELECT COUNT(*) AS NUM,EXTRACT(YEAR FROM BDATE) AS BIRTH_YEAR
FROM EMPLOYEE
GROUP BY BIRTH_YEAR;

SELECT SSN, SALARY, 
	CASE 
		WHEN SALARY<20000
			THEN "LESSS"
		WHEN SALARY=20000
			THEN "EQUAL"
		ELSE
			"MORE"
	END AS INTEL
FROM EMPLOYEE;
			





